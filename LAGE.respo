# LAGE.respo – Position / Stabilität / 12e / 6D

LAGE = /lage/

ROOT.compat  = TMP
ACHSE.compat = TMP
TMP.compat   = TMP
LAGE.compat  = TMP
laTER.compat = TMP
ORT.compat   = TMP

FREQ = 12e

D3 = LAGE

SEQ.360 = D3 → D4 → D5 → D0 → D1 → D2 → D3

ACTIVE = TRUE
